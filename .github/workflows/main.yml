name: Hello World Manual Trigger

on:  
  workflow_dispatch:  # Enables manual trigger

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Echo Hello World
        run: echo "Hello World"
